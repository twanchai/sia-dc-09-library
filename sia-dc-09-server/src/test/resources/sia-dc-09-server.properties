# SIA DC-09 server configuration

# Bind address (0.0.0.0 = all interfaces)
server.bind=0.0.0.0

# Primary listen port (TCP+UDP)
server.port.primary=50005

# Secondary listen port (TCP+UDP)
server.port.secondary=50006
